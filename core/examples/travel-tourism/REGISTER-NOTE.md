# Register note — travel-tourism (D-row, 5615 apex GAP) → #16 acquisition list

Facts filed from the 2026-08-23 full-economy property register row
`travel-tourism` and the 2026-08-23 property-surface register. No advocacy;
flags carried verbatim.

## The gap and the ruled posture

- **GAP at the 5615 apex**: no category-completing TLD and no api.[category]
  held; the held apis.* set names sub-verticals only (register row
  `proposed_primary_name`). No apex candidate is named in any estate doc; if
  an apex name is wanted it is an acquisition-list item per the ladder
  terminus (#16). Nothing here implies a purchase.
- **Ruled posture is explicitly plural** (the one vertical row where it is):
  per-sub-vertical properties on the held names — apis.cruises / apis.voyage /
  apis.camp already carry CF zones (cheapest Axis-2 starts) — rather than one
  apex property. This build follows it: ONE substrate under the row key,
  per-sub-vertical G4 brand configs recorded beside it
  (`projections/apis.{cruises,voyage,camp}.json`), the served face on the
  placeholder address `travel-tourism.org.ai` per spec §0 (the G3 work is
  never blocked on a name).
- **Held names without configs** (recorded, deferred): **apis.flights** and
  **apis.vacations** are held name-only (no zone, porkbun/2027-07-26).
  Consumer flights/vacations are inside the row's ruled avoid-class (consumer
  travel = "most crowded agent lanes", avoid-class 5, SC #22), and no zone is
  provisioned — writing offer/pricing/experiment configs for them today would
  be speculation, not record. They stay held; configs attach if/when a
  sub-vertical posture is ruled for them.
- **apis.holiday** is held but split to the LODGING row by the register's cut
  (coverage B.1 had it under Travel) — recorded register-cut, not a holding
  ambiguity; not touched here.
- **apis.taxi / apis.limo** sit in the same coverage A.2 group but are 4853
  mobility niches counted under Transportation — excluded from this row by
  the register; not touched here.

## Shared-face / primacy check (batch watch list)

The full-economy register was checked for a projection-primacy ruling on any
row sharing a record type or same-x name with this one: **none exists** (no
`primacy` ruling appears in either register). Per the watch-list rule, this
build stays under its own row key and claims nothing shared. Collisions
recorded, not resolved:

- **Booking/Reservation record grain** is shared vocabulary with the
  `lodging` row (schema.org Offer/Reservation, SC #22) and the
  `passenger-mobility` row (booking/trip records derived by analogy to
  SC #22). This build's `Booking` noun is typed under the travel-tourism
  substrate only; no cross-row Booking definition is claimed or exported.
- **Trip record grain** likewise adjacent to passenger-mobility [that row's
  data ply is itself flagged UNVERIFIED]. Same treatment: row-local typing.

## Source-route honesty (probe rule)

The row's route is Axis-2 first-party booking capture at the rail
(owned-by-construction) pointed at the unserved sub-verticals; incumbency per
sub-vertical is [UNVERIFIED — needs the shared probe]. That route is not
reachable in-session, and **no class-A source exists for this row today** —
so the wave-zero sandbox corpus is **labeled synthetic seed per spec §5.2**
(fictional operators/vessels/ports, role labels for people, 00-prefix
registration ids), never improvised class-A status. Fare/availability data is
deliberately absent: the row rules it the crowded lane (avoid-class 5).

## Standing flags carried (not resolved here)

- IATA NDC/ONE Order interchange rails: [UNVERIFIED — not in any estate doc];
  NOT adopted — schema.org Reservation/Trip typing per cascade rule 2.
- Sub-vertical record grains (sailing/charter manifest; session/enrollment
  roster): [UNVERIFIED — inferred]; carried onto the noun bindingNotes.
- 4872 sub-code, headless 52-System coordinates, agency mid-office:
  [UNVERIFIED] per the row; carried verbatim.
- Grain question open for the curated register: the plural posture is
  consistent with the property-grain ruling only if each sub-vertical is
  treated as its own grammar-nameable category [row notes; analysis].
