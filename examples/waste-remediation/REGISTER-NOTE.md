# Register note — waste-remediation (GAP row) → #16 acquisition list

Facts filed from the 2026-08-23 full-economy property register row
`waste-remediation` and the 2026-08-23 property-surface register. No
advocacy; flags carried verbatim.

## The gap

- The row holds **zero names anywhere in the estate** — the coverage doc's
  verdict is explicit ("GAP — zero names", B.1 row 56b), and the category
  appears in C.1's list of cells with no named candidate at all (with
  Mining/O&G, Utilities, Telecom, and three Functions).
- Ladder-order acquisition targets per the row: a category-completing TLD or
  api.waste / api.hauling-class names [UNVERIFIED — no candidate name appears
  in any estate doc; acquisitions file on #16; write-as-if-produced notes no
  purchase is implied here].
- Surface-register gap-fill candidate: **waste.dev** (the register's `.dev`
  gap-fill route note: standard registrations ~$12–15/yr; availability
  [UNVERIFIED]; no purchase implied by the listing).
- Adjacency, not candidates: the compliance micro-check .dev set
  (fifra/neshap) names statutes, not this market [row, verbatim].

## What was built without the name

Per template spec §0 (GAP rows) and open question 4, the G3 substrate was
instantiated **G3-first under the placeholder address
`waste-remediation.example.com.ai`** (this directory): machine face at the
ratified pin (native axp-ext-rates-g2@0.2.0), both plies from one definition,
labeled synthetic sandbox + **real ingested EPA ECHO Facility corpus** (the
row's public-licensable source route, probed in-session: ECHO keyless 200,
e-Manifest 401), stub rate card, seams. The G4 brand config is intentionally
absent — `projection.placeholder.json` carries `brand: null` and a
placeholder projection so the experiment registration and seams are real
before naming. Attaching an acquired name is a config change, not a rebuild.

## Source-route probe results filed back against the row's [UNVERIFIED] flags

- **EPA ECHO RCRA facility search: VERIFIED reachable** — keyless public
  REST, real RCRA handler records, public domain (17 U.S.C. § 105).
  The row's "public-licensable ingest" class is confirmed for the facility
  grain (2026-08-23 probe, this build's `scripts/ingest-facilities.mjs`).
- **EPA e-Manifest / RCRAInfo REST: reachable but auth-gated** — `401`
  without registered credentials (2026-08-23 probe). The row's statement
  that e-Manifest is "already a mandatory digital record" remains
  [UNVERIFIED] as written; what is verified is only that the API exists and
  requires registration. The e-Manifest ingest is the first rung of this
  row's enrichment ladder, not a wave-zero input.

## Shared-face / collision check (batch watch-list item)

No projection-primacy ruling exists in either register for this row, and the
row shares no name or substrate with any other row (it holds no names at
all). The nearest derived convergents named by the row — the chemical
SDS/hazmat and recall/traceback rails (VC #25/#28) — belong to OTHER rows'
theses; this build **claims neither** and records the adjacency only.

## Rail-ledger status (batch watch-list item)

**BLOCKED-ON-RAIL-LEDGER.** `~/projects/ax` branch `draft/rail-ledger-v1`
holds only an uncommitted `packages/rail-ledger` skeleton as of 2026-08-23
(empty `registry/`, no `LEDGER.md`, no address convention committed).
§9.1's final box (face registered in the rail ledger) is recorded blocked,
not stubbed.

## Open question carried from the row (not ruled anywhere)

The row's demand side is universal-input-row shaped [UNVERIFIED inference
kept]: every manufacturer and construction site is a waste generator with
manifest obligations. If that inference holds, the cell's principal buyers
are non-technical (generator compliance staff) and the motion is
B2A2B-shaped — which makes the missing name TWO missing names (a machine
face and a human-vocabulary counterpart brand). Acquisition scoping on #16
should decide whether the cell is entered developer-first or
counterpart-first before any name is bought.
