# REGISTER-NOTE — `mining-oil-gas` wave zero (GAP row, batch 4)

**Row:** `mining-oil-gas` — Mining, Oil & Gas (NAICS 21), vertical, cluster 1.
**Register:** `studio/docs/plans/registers/2026-08-23-full-economy-property-register.json`.
**Build date:** 2026-08-23 · **Branch:** `draft/mining-oil-gas-wave0`.

## GAP status (why this is a placeholder)

The row is a GAP at every naming-ladder rung: no category-completing TLD, no
`api./apis./headless./data.` form held, no acquisition candidate in any estate
doc, and no WATCH row for NAICS 21 (register: "GAP — ladder exhausts").
Held names are **artifact singletons only** — `minesafety.dev`
(regulation-check grammar), `jib.claims` (JIB document grammar),
`tankcheck.dev`. Per spec §0 and the batch-1/2 D-row precedent
(`examples/manufacturing`, `examples/fn-strategy`), the G3 substrate + anon
sandbox are built under the **row-key placeholder address**
(`mining-oil-gas.org.ai`); the **G4 brand config is withheld** pending #16
acquisition. Naming is never re-derived here.

- Surface-register gap-fill note lists `mining.dev` / `oilfield.dev` as cheap
  `.dev` candidates — **availability [UNVERIFIED], no purchase implied**.

## Analysis-thin discipline

The row states: "everything beyond the three artifact names is inference."
This build therefore claims only what the row states, and marks what the row
marks: the Nouns derive from the named artifact grammars (JIB statement) and
the named public feeds (state well registries, MSHA, FracFocus); the System
set (production accounting/JIB, EAM) carries the row's own [UNVERIFIED] flag;
PIDX is not encoded anywhere (row marks it [UNVERIFIED — not in any estate
doc]). The concrete next facts the row itself names — an incumbency probe and
a name — remain absent and are not simulated here.

## Source-route probe (honest, in-session, 2026-08-23)

The row's route is public-licensable ingest, recorded **[UNVERIFIED — none
probed]** and carrying **no class-A rating**. Probed this session (HTTP GET,
reachability only — no ingest attempted):

| Feed | URL probed | Result |
|---|---|---|
| MSHA open data (arlweb) | `https://arlweb.msha.gov/OpenGovernmentData/OGIMSHA.asp` | 200, text/html (index page) |
| MSHA data & reports | `https://www.msha.gov/data-and-reports` | 200, text/html |
| FracFocus download | `https://fracfocus.org/data-download` | 200, text/html |

All three index pages are reachable. That is **reachability, not a class-A
rating** — per the batch watch list, real data rides only a class-A-rated
route, so the wave-zero sandbox seed is **labeled synthetic** (§5.2)
throughout: `example: true`, `[demo]` prefixes, synthetic `00-` state-prefix
well API numbers, regulatory classification codes (30 CFR cites, CAS numbers)
omitted rather than fabricated. Upgrading Well/Violation/Disclosure bindings
from `generated` to `ingested` requires the route to be probed to depth and
rated upstream — a register fact, not a build decision.

## Shared-face check (batch watch list)

Register scan on 2026-08-23: no other row shares a name or substrate with
`mining-oil-gas` — the row holds no names, and its only cross-reference
(commodity price data) is recorded upstream as a universal-input row (VC #15),
not a shared face. **No projection-primacy ruling needed; no collision to
record.** Built under this row's own key throughout.

## Counterpart-brand gap (spec §5.1, recorded)

The row's ICP personas (land/JIB accountant, EHS/compliance manager) are
non-technical principals and no human-vocabulary apex name is held — recorded
as a counterpart-brand gap in `projection.js` (`counterpartBrand.gap: true`).
Wave zero serves the B2A motion only, so no counterpart surface exists yet;
the §9.3 diagnostic (agent-referred human traffic, wired at the seams) or the
G4 brand attachment triggers the counterpart proposal, filed against the
register row and named per #3.

## Platform-account registration (§9.1 final box)

**blocked-on-rail-ledger.** Probed `~/projects/ax` branch
`draft/rail-ledger-v1` (worktree HEAD `1620e9f`, 2026-08-23): no ledger
service, no `LEDGER.md`, no address convention exists on the committed branch
yet. Recorded as BLOCKED in `scripts/selfcheck.mjs` box 16 — never stubbed.
When the ledger's address convention lands, point box 16 at it.

## What attaches when a name is acquired (#16)

The G4 projection config (`projection.js`) swaps `brand: null` for the
acquired name, the placeholder origin CNAMEs per §7, and the counterpart-brand
question re-opens with the ICP. Nothing in the G3 substrate changes.
