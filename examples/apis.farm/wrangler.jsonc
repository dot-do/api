{
  // apis.farm — wave-zero worker (custom site allowed per template §7.1
  // prove-then-extract; CNAME → workers.do is the extraction target, not a
  // wave-zero requirement). NOTE: apis.farm has NO CF zone — zone
  // provisioning is a Batch-S hours-class admin item; it blocks serving,
  // not building. `wrangler dev` runs locally today.
  "name": "apis-farm",
  "main": "src/worker.ts",
  "compatibility_date": "2026-08-01"
}
