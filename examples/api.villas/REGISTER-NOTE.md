# Register note — lodging → api.villas (wave zero)

Facts filed from the 2026-08-23 full-economy property register row `lodging`
and the 2026-08-23 property-surface register. No advocacy; flags carried
verbatim.

## Name and apex GAP

- Built under **api.villas** — the row's proposed primary name, ladder step 2
  within what is held (no category-completing TLD; api.[x] held). Surface
  register: held, porkbun, expiry 2027-07-26, **NO Cloudflare zone**
  (state name-only), grammar pass ("object/act domain of the rail").
- The row's caveat is carried, not resolved: **'villas' names a lodging
  sub-niche, not 7211 hotels** — the category-apex name (api.hotel-class) is
  unheld. The **7211 apex GAP is recorded per the register** as a register
  note, not a build blocker; this build claims only the sub-niche rail.
- Face sibling **apis.villas** (held, name-only) completes the ratified
  hold pair; **apis.holiday** (zone y) sits on this row by the register's
  cut — flagged there against the coverage doc's Travel placement — and is
  NOT served by this build. **nightaudit.click** is the row's artifact door
  (see counterpart-brand note in `projection.api.villas.json`).

## Source-route honesty (batch watch-list item)

The row is **Axis-2 only by ruling** (avoid-class 5 — "most crowded agent
lanes", SC #22). Probe result filed honestly:

- The ruled route — **first-party booking/night-audit capture at the rail**
  (owned-by-construction class) — is a **C-class, unbuilt rail** at wave
  zero (batch 7). An unbuilt first-party capture rail cannot be probed into
  existence; there is nothing external on this route to probe.
- The only other lane, **public rate data, is the register-ruled avoid
  lane** and was deliberately NOT probed as a source.
- Consequence: **no Class-A source is honestly reachable on this row's
  route**; every record on this face is labeled synthetic §5.2 seed, and no
  class-A status is improvised anywhere (`check.mjs` gates this: no record
  may carry `real: true` or a provenance block).
- The row records **no enrichment ladder** — nothing was invented to fill
  that field (spec §0: a missing field is a register defect, not an
  improvisation license). The natural first rung — operator-claimed
  workspaces feeding real first-party bookings — is an observation for the
  register, not a route this build claims.

## Shared-record-type collision (batch watch-list PRIMACY item)

**No primacy ruling exists** in either register for the Booking/Reservation
record types shared across batch-7 rows (checked 2026-08-23: zero `primacy`
hits in both register JSONs). The travel-tourism row's data ply names
"Booking/Reservation record (schema.org-typed)" — the same grain this row's
verbatim anchor names ("Booking record (schema.org Offer/Reservation)").
Per the watch-list default this build:

- builds under **its own row key `lodging`**;
- scopes its Booking noun to the lodging grain
  (`https://schema.org/LodgingReservation`, stay/night semantics);
- **claims nothing shared** and asserts no cross-row ownership of the
  Booking/Reservation type;
- records the collision here and in `projection.api.villas.json` for the
  primacy ruling to supersede.

## Rail-ledger registration (batch watch-list item)

**REGISTERED (door A).** `~/projects/ax` branch `draft/rail-ledger-v1`
(≥ 3ec5bb6) holds the committed rail-ledger service and durable face
registry. This build added its one-row registration for `api.villas` to
`packages/rail-ledger/registry/faces.json` and records
`"railLedger": "https://ledger.apis.ax/faces?face=api.villas"` in the
projection config. §9.1 box 16 discharged — not blockable, not stubbed.

## [UNVERIFIED] flags carried, none upgraded

- HTNG/OTA hospitality interchange standards [UNVERIFIED — not in any
  estate doc]: no keyless interchange feed exists on the row's route, so
  record typing follows the row's verbatim schema.org anchor + cascade
  rule 2. Nothing here verifies or refutes the flag.
- ARI rate/availability records and channel-manager function [UNVERIFIED —
  inferred]: **not built and not declared** (presence-when-true) — serving
  a rate/availability surface would also brush the register-ruled avoid
  lane, so the flag is left standing for the curated register.
- Folio/guest-ledger record [UNVERIFIED — inferred]: built as the smallest
  honest reading (one folio per checked-out example booking, labeled
  synthetic), flag kept verbatim on the noun.
- 721214 recreational-camp adjacency [UNVERIFIED sub-codes]: untouched;
  camps sit with the travel-tourism row's held names (apis.camp).
