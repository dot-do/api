/**
 * GENERATED FILE — do not edit by hand. Regenerate: node scripts/gen-icons.mjs
 * UI icons as build-time-inlined SVG (Iconify data, lucide collection).
 * Estate rule (api.qa icons.ts / studio site-v3): no hand-authored glyph
 * path data, no unicode symbols as icons, zero runtime icon fetches.
 */
/** lucide "menu" — inline SVG resolved at build time; sized 20px, colored by currentColor. */
export function menuIcon(cls = 'icon') {
  return `<svg class="${cls}" width="20" height="20" viewBox="0 0 24 24" aria-hidden="true"><path fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 5h16M4 12h16M4 19h16"/></svg>`
}

/** lucide "x" — inline SVG resolved at build time; sized 16px, colored by currentColor. */
export function closeIcon(cls = 'icon') {
  return `<svg class="${cls}" width="16" height="16" viewBox="0 0 24 24" aria-hidden="true"><path fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 6L6 18M6 6l12 12"/></svg>`
}
