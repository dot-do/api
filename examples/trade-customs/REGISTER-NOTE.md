# Register note — trade-customs (GAP row) → #16 acquisition list

Facts filed from the 2026-08-23 full-economy property register row
`trade-customs` and the 2026-08-23 property-surface register. No advocacy;
flags carried verbatim.

## The gap

- The row holds **no name in api./apis./headless./data. grammar** and no
  category-completing TLD (register row `held_names`: none matches this
  category in the coverage doc; apis.flights/apis.cruises are passenger
  travel, not cargo — excluded).
- Candidates already written into estate docs, ALL availability-**UNVERIFIED**:
  **api.trade†, api.customs†, tradedocs†** [MAP §2 #6 via coverage C.1 #8].
  Nothing here implies a purchase.
- Surface-register gap-fill candidates: **customs.dev / tradedocs.dev** (the
  register's `.dev` gap-fill route note: standard registrations ~$12–15/yr;
  availability [UNVERIFIED]; no purchase implied by the listing).
- G4 attachment held pending **#16** (GAP-row acquisition list).

## Ruled posture and standing flags (carried, not resolved here)

- **SC #4 posture: "Class E, claim-now-build-later"** — the document-layer
  vacancy claim is staged, not acted on. This build changes nothing about
  that posture; it instantiates the G3 substrate the posture already assumes.
- **Incumbency-probe prerequisite stands** [matrix §5 tension 7]: the
  standing incumbency probe for this cell has not been run; it remains a
  blocker to acting on the vacancy claim and is unaffected by this wave-zero
  build.
- **Regulated-act separation**: customs brokerage (19 CFR 111 licensure) is
  cleanly separated from the buildable document layer; the operator brings
  the license (#22 regulation unlock). Recorded in `projection.json` and
  enforced on the surface (broker-side scopes answer BLOCKED).
- **Statutory clock**: MLETR/ETDA adoption spread converts paper trade
  documents into typed-data demand per jurisdiction — dated demand, recorded
  on the row; carried onto the eBL records as typing metadata.
- **[UNVERIFIED] anchors carried verbatim**: WCO HS/HTS classification spine
  and X12/EDIFACT customs transaction sets are not cited in estate docs; the
  customs-entry grain is flagged [UNVERIFIED] by the row. The sandbox uses
  synthetic HTS-SHAPED codes only.

## Shared-face check (batch watch list)

The register was checked for a projection-primacy ruling on any row sharing
a name or substrate with this one: none exists. No name is held or claimed;
the build lives entirely under the row's own placeholder key
(`trade-customs`). Adjacency noted, not claimed: the `freight-logistics` row
(separate key) covers the movement grain; this row's grain is the typed
document layer — the two lenses that nominated this cell (SC+VC) converge on
the document grain [matrix §1 trade-docs row]. No collision to record.

## What was built without the name

Per template spec §0 (GAP rows) and open question 4, the G3 substrate was
instantiated **G3-first under the placeholder address
`trade-customs.org.ai`** (this directory): machine face at the ratified pin
(+ native axp-ext-rates-g2@0.2.0), both plies from one definition, labeled
synthetic sandbox seeded from the row's spec-native anchors (UN/CEFACT +
DCSA typing), stub rate card, seams. The G4 brand config is intentionally
absent — `projection.json` carries `brand: null` and a placeholder
projection so the experiment registration and seams are real before naming.
Attaching an acquired name is a config change, not a rebuild.
