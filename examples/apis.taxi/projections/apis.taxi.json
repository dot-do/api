{
  "$comment": "G4 projection config (spec §2) — Projection 1 of the passenger-mobility substrate. Content, not code. SERVED by this example worker (zone provisioning for apis.taxi is pending — Batch-S admin; blocks serving at the apex name, never building).",
  "substrate": "passenger-mobility",
  "brand": "apis.taxi",
  "domains": ["apis.taxi"],
  "served": true,
  "icp": {
    "companyTypes": ["livery/limo operator", "shuttle/charter operator", "NEMT provider", "taxi fleet"],
    "jobTypes": ["fleet owner-operator", "dispatch manager"]
  },
  "personas": [
    { "id": "operator-fleet-owner", "role": "fleet owner-operator running dispatch and bookings" },
    { "id": "customer-dispatch-systems-developer", "role": "developer at a dispatch/booking systems vendor integrating the functions catalog" }
  ],
  "motion": "B2D",
  "offer": [
    { "shape": "anon-sandbox", "gate": "none — keyless", "price": "free" },
    { "shape": "oauth-free-tier", "gate": "GitHub OAuth (labeled demo mode until OAuth credentials are configured; keys unpersisted, not yet enforced)", "price": "free quota" },
    { "shape": "self-serve-metered", "gate": "key + card on file; 402 boundary — settlement rail not yet activated (A1 charter); the 402 OFFER + checkout seam is served as a labeled stub", "price": "metered per rate card" },
    { "shape": "committed-subscription", "gate": "self-serve signup + commitment", "price": "declared in a future rate-card revision; not served today (presence-when-true: no tier row is published until it exists)" }
  ],
  "pricing": {
    "pattern": "402-metered-per-call",
    "rateCardRef": "/pricing",
    "note": "metered per-call on quoted fares + a per-outcome row on dispatched trips (the outcome grain of the dispatch door); all record reads free at wave zero"
  },
  "positioning": "the functions a passenger fleet's systems call",
  "positioningNote": "agent-default claim WITHHELD until the §4.6 worthiness bar passes (conformance + live anon sandbox + verified published suite with runs on record)",
  "mdx": "custom landing in this worker (spec §7.1: custom site allowed at wave zero; per-brand MDX layer on the shared renderer deferred to extraction)",
  "experiment": {
    "pattern": "402-metered-per-call",
    "motion": "B2D",
    "shapes": ["anon-sandbox", "oauth-free-tier", "self-serve-metered", "committed-subscription"],
    "rateCardRef": "/pricing",
    "startDate": "2026-08-23",
    "hypothesis": "per-outcome dispatch pricing beats per-call metering on revenue per substrate call for fleet operators"
  },
  "counterpartBrand": {
    "state": "GAP-RECORDED",
    "candidate": null,
    "gap": "the register row's consumer side is B2A2C free-rider only (consumer-services posture), and the estate holds NO human-vocabulary name for consumer ride booking in the 485-487 cell — apis.taxi and apis.limo are both developer-grammar names. A counterpart-brand proposal is filed against the register row when the §9.3 signal (agent-referred human traffic on this developer brand) fires; naming per #3, never API-vocabulary patches on this surface.",
    "trigger": "the §9.3 diagnostic — agent-referred human traffic arriving on this developer brand"
  }
}
