# Register note — fn-corporate-affairs (GAP row) → #16 acquisition list

Facts filed from the 2026-08-23 full-economy property register row
`fn-corporate-affairs` and the 2026-08-23 property-surface register. No
advocacy; flags carried verbatim.

## The gap

- The row holds **zero estate names of any kind** (row anchor: "GAP — zero
  names held"; B.2 verdict: no api-grammar name, no category-completing name,
  no artifact names). It is one of the two Function rows (with Strategy)
  holding literally none, and one of the "3 Functions unnamed" cited in the
  #3 ladder's gap note.
- The row's own naming ladder terminates at "acquire something better," which
  files to the #16 acquisition list as analysis, not action.
- Surface-register gap-fill candidate: **corporateaffairs.dev** (the
  register's `.dev` gap-fill route note: standard registrations ~$12–15/yr;
  availability [UNVERIFIED]; fit flagged [UNVERIFIED] by the register itself;
  no purchase implied by the listing).

## Decomposition check (the row's own question, answered at wave zero)

The row warns its record grain may decompose entirely into records other
properties already own, "which would make the GAP benign." Checked before
minting record types:

| Row slice | Finding |
|---|---|
| Board/governance artifacts (minutes, resolutions, consents) | **Decomposes.** Owned by the entity back-office convergent (holdings-corporate-mgmt substrate; api.holdings register row, zone-only). No GovernanceArtifact Noun minted here. |
| Public filings (lobbying disclosures, proxy/IR corpora) | **Decomposes as corpus, not as view.** The public-record read-layer convergent owns the corpus; this substrate carries a `Disclosure` Noun bound `federated` (a corporate-affairs view), unwired and synthetic at wave zero. Source route is register inference only [UNVERIFIED]. |
| Stakeholder relationship register + engagement log | **Does NOT decompose.** No other register row serves the stakeholder/IR-CRM grain (investors, regulators, media, community + the engagement log). Minted here as the native Nouns (Stakeholder, Engagement). |
| Press/mention records | **Does not decompose** to a named owner; the press-index source route is [UNVERIFIED]. Minted as a `generated` Noun (synthetic seed at wave zero). |

**Net: the GAP is not fully benign.** A residual stakeholder/engagement grain
exists that no other property owns. Whether that residual justifies a
standalone acquisition or folds into an adjacent property is a founder
question, not answered here.

## What was built without the name

Per template spec §0 (GAP rows) and open question 4, the G3 substrate was
instantiated **G3-first under the placeholder address
`fn-corporate-affairs.org.ai`** (this directory): machine face at the ratified
pin with the batch-2 ruled extension placements (top-level `rates[]`,
top-level `g2`, `links.verify`, operationId on every route), both plies from
one definition, labeled synthetic sandbox, stub rate card, seams. The G4 brand
config is intentionally absent — `projection.config.json` carries
`brand: null` and a placeholder projection so the experiment registration and
seams are real before naming. Attaching an acquired name is a config change,
not a rebuild.

## Flags carried from the row (not softened)

- The headless-ply system derivation is **unrun**: no system-of-record for
  this function is named in the 52-System catalog excerpts; the declared
  `StakeholderRelations` coordinate is a candidate-by-analogy, flagged
  [UNVERIFIED] in `substrate.js`.
- The ICP skew (largest-company bias of the 13 Function rows) is
  [UNVERIFIED — inference from the function's nature, not from atlas data].
- The public-filings source route is [UNVERIFIED inference]; nothing class A
  is reachable, so the §5.2 labeled synthetic path applies by rule.
