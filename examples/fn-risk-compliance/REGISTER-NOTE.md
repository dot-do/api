# Register note — fn-risk-compliance (GAP at the umbrella grain) → #16 acquisition list

Facts filed from the 2026-08-23 full-economy property register row
`fn-risk-compliance` and the 2026-08-23 property-surface register. No
advocacy; flags carried verbatim.

## The structure (the row's own words)

"Structurally the inverse of every other row: held at the narrowest grain
(per statute) with a gap at the property grain." Two readings coexist in the
register, unresolved by it and not resolved here: the .dev family IS the
product shape (the DC lens's regulation decomposition — verdict (a) per
statute [H]) and the function-level name is only an umbrella; or the umbrella
is a real missing property. Resolution is a #16/#3 curation question
(#33 Q6 per the surface register: umbrella vs per-statute family, pending).

## The gap

- **No api-grammar name held** at function level: api.compliance /
  apis.compliance not held; headless./data. forms not held; no
  category-completing TLD at function breadth.
- **compliance.do + soc2.do are held but convention-barred** (.do =
  primitives, never startup brands — convention 6). Not used here.
- **apis.claims is insurance-row adjacency**, not this row's name. Not used.
- The register "records the gap; it does not name a phantom" — kept: this
  build names no umbrella anywhere.

## The held grain (what the family actually owns)

28 per-statute .dev fronts, enumerated by the property-surface register
(family `.dev-instrument`, substrate `fn-risk-compliance`, registrar porkbun,
expiries 2027-07-28/30, zone-only except edi834.dev name-only):
batterycheck, bottledeposit, carriercomp, ciac, cleantruck, datecode,
davisbacon, dirreg, edi834, emrcheck, fflcheck, fifra, hospicecap,
jointcheck, lawlabel, ld2, mattressfee, minesafety, neshap, payonscan,
pilotrecords, regf, rxverify, snfcb, tankcheck, taxhome, tirefee,
trustnotice (.dev each). The full-economy row's census said "~20" with the
list beyond five recited names flagged `[UNVERIFIED]` at name level; the
surface register's enumeration (28) is the operative inventory and is what
`seed.js` serves as the family register. Front→statute bindings exist only
for the five recited fronts; the other 23 bindings are a curation act, left
null-flagged.

## What was built without the umbrella name

Per template spec §0 (GAP rows) and the register's family-grain provisioning
note ("one pipeline, ~20 fronts"), the G3 substrate was instantiated
G3-first under the placeholder address `fn-risk-compliance.org.ai` (this
directory): machine face at the ratified pin (extension rates-g2 native),
both plies from one definition (the shared check-runner is the headless
door), statute/register reference records with honest provenance, labeled
synthetic results, stub rate card, seams, rail-ledger registration. The G4
brand config is intentionally absent — `projection.config.json` carries
`brand: null`. Attaching an acquired umbrella name OR any held .dev front is
a config change, not a rebuild.

## Consumption edge (not a blocker)

Attested-check output rides the api.qa verification rail (VerificationReport
abstraction, H4 — LIVE; fn-it / Batch S). Wave zero consumes nothing yet:
every run is `attested: false` and no verdict URL is fabricated. The edge is
typed and declared in `projection.config.json`.

## Primacy / collision record

No primacy ruling found in either register for this row's record types or
names. Collisions recorded (build under own row key, nothing claimed
shared): Construction (NAICS 23) owns the draw/lien-waiver/pay-app
payment-documentation set; jointcheck.dev / trustnotice.dev are
construction-payment-adjacent HELD fronts whose record typing is deferred to
curation. apis.claims not referenced by any served surface.
