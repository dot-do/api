# Register note — fn-strategy (GAP row) → #16 acquisition list

Facts filed from the 2026-08-23 full-economy property register row `fn-strategy`
and the 2026-08-23 property-surface register. No advocacy; flags carried verbatim.

## The gap

- The row holds **no name in api./apis./headless./data. grammar** (coverage doc B.2,
  Strategy row: "GAP (no api-grammar name; .do primitives only)"). The referenced
  .do primitives are unenumerated in the coverage doc itself.
- The naming ladder exhausts unheld: no category-completing TLD exists for the
  function; `api.strategy` / `apis.strategy` are not held — availability
  [UNVERIFIED], neither appears in WATCH or C.1's named-candidate list.
- Surface-register gap-fill candidate: **strategy.dev** (the register's `.dev`
  gap-fill route note: standard registrations ~$12–15/yr; availability
  [UNVERIFIED]; no purchase implied by the listing).

## What was built without the name

Per template spec §0 (GAP rows) and open question 4, the G3 substrate was
instantiated **G3-first under the placeholder address `fn-strategy.org.ai`**
(this directory): machine face at the ratified pin, both plies from one
definition, labeled synthetic sandbox, stub rate card, seams. The G4 brand
config is intentionally absent — `projection.config.json` carries `brand: null`
and a placeholder projection so the experiment registration and seams are real
before naming. Attaching an acquired name is a config change, not a rebuild.

## Open question carried from the row (not ruled anywhere)

The estate's strategy "product" may be constitutively internal — the studio IS
the strategy function for its portfolio — which would make this row
internal-only (like media) rather than an acquisition gap. The row's nearest
live asset is not a brand: the estate atlas (industries × occupations × tasks ×
processes × automatability) is the strategy-function data instrument, ruled a
platform capability. Acquisition of a name for this row should follow a ruling
on that question, in whichever direction it lands.
