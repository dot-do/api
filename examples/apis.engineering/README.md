# apis.engineering — wave zero (register row `engineering-architecture`)

Instantiated from the 2026-08-23 full-economy property register per the
property-template spec (`studio/docs/plans/2026-08-23-property-template-spec.md`).
Row state at build time: **Nominal** — names held, zero thesis, nothing serving.

## What serves

One worker, one substrate, two plies:

- **Data face:** `/drawings` (branching collection, `?discipline=`, `?tag=`),
  `/drawings/{id}`, `/specifications`, `/submittals` — drawing / spec /
  submittal records on schema.org generics (cascade rule 2: no settled
  interchange standard is cited for this cell in the estate record).
- **Headless face:** `/projects` — auto-minted keyless sandbox projects with
  submittal assembly (`POST /projects/{id}/submittals`), native binding, same
  nouns, same envelopes.
- AXP quartet + `/offer` + `/verify` + `/icp.json` + MCP door (`POST /mcp`).

Machine face generated by **vendored axp-faces 0.3.0** (pinned
`apis-ax-axp@2.6.0`, digest `a9a1197c…`; extension `axp-ext-rates-g2@0.2.0`,
digest `903e414d…`), vendored byte-identical via `git show` from the
axp.org.ai repo's committed HEAD `523c9ef217d54feefb0b20734a6d2996a6965b79`
on branch `draft/axp-extension-rates-g2` — see `src/axp-faces/VENDORED.json`.
The four extension fields (top-level `rates[]`, top-level `g2`,
`links.verify`, per-route `operationId`) are native manifest inputs; no
site-side bridges.

## Data class — read this first

**Every record on this origin is labeled synthetic example data** over
fictional firms and a fictional project (`"example": true` on every record,
estate fixture law). The row's source route has not been derived to ingest
depth — no corpus is named anywhere in the estate record — so the §5.2
labeled synthetic sandbox seed is the honest wave-zero data class. Nothing
here implies a real project, firm, or licensed artifact.

## Boundaries, deliberately kept

- **PE-stamp licensure floor:** stamped artifacts are reserved acts of
  independent licensed professionals. Every record carries `stamped: false`;
  the `stamped` collection scope answers `403 BLOCKED` by construction.
- **NAICS 23 boundary:** the draw / lien-waiver paperwork family belongs to
  Construction and is not served, declared, or seeded here.

## Name collision, recorded (no projection-primacy ruling found)

`~/projects/ax/packages/api.engineering` is an **ADR-0020 directory data
home on the singular twin** (`api.engineering` — the same-x pair's demand
rail, #3 rule 1; open-floor/BYOL AEC capability directory on the
agent-experience substrate). No projection-primacy ruling exists in the
register for this shared substrate. Per the batch rule this build claims
**only the row-key face `apis.engineering`** and does not serve, redirect,
or speak for `api.engineering`. A primacy ruling remains open for the
founder.

## Checklist state (§9.1, run `node scripts/selfcheck.mjs`)

**15/16 boxes pass.** Box 16 (platform-account registration) is recorded
**blocked-on-rail-ledger**: at build time the ledger service on
`~/projects/ax` branch `draft/rail-ledger-v1` has an empty `registry/`, no
committed `LEDGER.md`, and no published face-address convention. Register
this face there the day the convention lands — never stub it.

## Deploy notes

No Cloudflare zone exists for apis.engineering (surface register:
`cf_zone: n`; held at Dynadot, name-only). Zone creation and nameserver
cutover are founder deploy-time acts; the wrangler route is commented out
until then (declaring it would be a ghost surface). Settlement is a labeled
test-mode stub: the 402 boundary is served, nothing is charged.
